# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Optional: Custom OpenAI API base URL
# Uncomment if using a compatible API (e.g., SiliconFlow, Azure OpenAI, local LLM)
# OPENAI_BASE_URL=https://api.openai.com/v1

# Optional: Model name
# Change this if using a different API provider
# For SiliconFlow: Qwen/Qwen2.5-7B-Instruct, deepseek-ai/DeepSeek-V3, etc.
# For OpenAI: gpt-4o-mini, gpt-4o, gpt-3.5-turbo
OPENAI_MODEL=gpt-4o-mini

# Optional: Embedding model (if using embeddings)
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small
